# World Card — Local Uncurated Saves

- **World ID:** `local-uncurated-saves`
- **Title:** Local Uncurated Saves
- **Classification:** ARCHIVED / ABANDONED
- **One-sentence promise:** A grab-bag of local `.saves/*.json` experiments that are not part of the curated, shipped collection.
- **Builder / owner:** various (Claude / Codex / experiments)
- **Branch:** main / local (not tracked as world branches)
- **Worktree:** none
- **Current commit:** n/a
- **Main status:** **Local save files only.** Not shipped, not curated.
- **Source assets:** Assorted `.saves/*.json` files that are not registered in `world-manifest.json` or bundled in `public/worlds/`. Snapshot of the truly-uncurated remainder: `canyon`, `citadel-preview`, `citadel-roam`, `citadel-v2`, `citadel-v3`, `default`, `flat-world`, `regular-user-moonspire` (legacy PR #40 play-mode demo, superseded by shipped `moonspire-realm`), `sh-scratch`. (Snapshot, not authoritative.)
- **Registry evidence:** Presence of these files under `.saves/` that do not correspond to shipped-world slugs **and** are not individually catalogued in the vault.
- **Current risks:** These are scratch/experiment saves. Do not treat any as a portfolio world without Edgar's review and promotion. This card is a catch-all, not per-file tracking.
- **Next required approval:** Edgar to decide if any individual save should be promoted to a tracked prototype.
- **Last verified date:** 2026-07-08

> **Reconciliation note:** Several `.saves` that *look* uncurated are actually named, restorable
> vault archives — **not** part of this catch-all. Per `Voxel Realm/World Archive.md`:
> `castle` → **The Grand Keep**, `town` → **Flatland Town**, `showcase` → **Showcase Village and
> Castle**, and the `codex-realm-circuit-*` saves → **Stonewood Circuit** (+ 2 continuations).
> `colosseum` and `codex-realm` also have their own cards/archives. This card covers only saves
> with no vault archive and no dedicated world card.

## Unknowns

- Provenance and intent of most individual scratch saves.
- Which (if any) are worth preserving vs. discarding — do not delete anything; that is Edgar's call.
